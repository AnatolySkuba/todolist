import styled from 'styled-components';
import { Button } from '@mui/material';
import { MARGINS } from '../../../theme';

export const LogoutButton = styled(Button)`
  margin: ${MARGINS.logoutButton};
`;
