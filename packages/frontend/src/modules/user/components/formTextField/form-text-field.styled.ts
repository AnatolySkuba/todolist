import styled from 'styled-components';
import { InputLabel } from '@mui/material';
import { MARGINS } from '../../../theme';

export const TextFieldInputLabel = styled(InputLabel)`
  margin: ${MARGINS.textFieldInputLabel};
`;
